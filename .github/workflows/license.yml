name: Check Licenses

on:
  workflow_dispatch:
  pull_request:
    branches:
     - 'main'
  issue_comment:
    types: [created]

jobs:
  license:
    uses: netomi/dash-licenses/.github/workflows/mavenLicenseCheck.yml@d3b7c0606dbd6fa1eec12536fca4dc1727627e25 # master
    permissions:
      contents: write
      issues: write
      pull-requests: write
      actions: write
    with:
      projectId: 'technology.cbi'
    secrets:
      gitlabAPIToken: ${{ secrets.GITLAB_API_TOKEN }}

